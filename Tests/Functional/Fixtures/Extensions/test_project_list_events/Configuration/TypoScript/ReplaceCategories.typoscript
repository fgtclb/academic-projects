# The list listener replaces the applicable categories with a single one, while leaving
# the projects alone - the filter of the plugin then offers only that category.
plugin.tx_academicprojects.settings.testProjectListCategory = 2
