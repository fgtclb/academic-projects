# A site package whose content element layout renders no header. Every plugin content
# element references `lib.contentElement`, so the path reaches all of them.
lib.contentElement.layoutRootPaths.50 = EXT:academic_projects/Tests/Functional/Plugins/Fixtures/Layouts/
