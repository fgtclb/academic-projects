# Aggregate page TSconfig: every component this extension ships.
#
# The counterpart of the set "fgtclb/academic-projects" for installations that
# include page TSconfig through the page field "Page TSconfig" instead of
# through a site set.
@import 'EXT:academic_projects/Configuration/TSconfig/ProjectList/page.tsconfig'
@import 'EXT:academic_projects/Configuration/TSconfig/ProjectListSingle/page.tsconfig'
